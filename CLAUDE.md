# Project instructions

## Skill versioning

- The `todo` skill carries a `version:` in `todo/skill/SKILL.md` frontmatter.
  **Whenever the skill changes (any edit to anything under `todo/skill/`), bump
  that version — at least a patch bump.** Do this in the same commit as the
  skill change; don't leave the version stale.
