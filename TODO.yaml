todos: [{id: init, title: INIT, type: feature, status: done, priority: medium,
        super-phase: null, created: 2026-06-23T22:56:51.609Z, completed:
        2026-06-24T00:16:37.044Z, notes: ["add an init or some sort of command that:\n\
            - adds the todo skill to ~/.agents/skills/\n- adds a symlink to ~/.claude/skills/\n\
            - whatever other initialization the cli needs", store a canonical
            copy of the todo skill that's currently in ~/.agents/skills/todo in
            this repo, init a new git repo and deploy to `aahl-byte/todo-cli`, "new
            status options\n- add a purple `review` status for items are specifically
            awaiting user review (not all todo-items go through this status, this
            is a special status where user review is requested)\n- add a red `blocked`
            status for items that are blocked by something or were unable to continue
            due to something", "Plan: (1) split statuses into todo/status.py + add
            review(purple)/blocked(red); (2) store canonical SKILL.md inside package
            at todo/skill/SKILL.md; (3) add 'todo init' to install skill + symlink;
            (4) deploy to aahl-byte/todo-cli (will confirm before pushing).", "Done:
            review(purple)/blocked(red) statuses + colors; canonical SKILL.md packaged
            at todo/skill/SKILL.md; 'todo init' installs skill + symlink. Shipped
            in 9f2ae29 (architecture refactor cc9734e landed first).", "Deploy to
            aahl-byte/todo-cli BLOCKED: gh CLI not authenticated, and creating a public
            repo + pushing is outward-facing — needs user 'gh auth login' + explicit
            go-ahead.", 'Deployed: public repo github.com/aahl-byte/todo-cli, origin
            remote added, master pushed.']}, {id: architecture, title:
        ARCHITECTURE, type: refactor, status: done, priority: medium,
        super-phase: null, created: 2026-06-23T22:59:49.451Z, completed:
        2026-06-23T23:24:01.313Z, notes: [not a fan of the monolith architecture,
        'Refactoring monolith cli.py (494 lines) into focused modules: util, status,
            yamlio, store, render, init, cli.', Split into
            util/status/yamlio/store/render/cli modules; behavior unchanged.
            Shipped in cc9734e.]}, {id: sub-tasks, title: SUB-TASKS, type:
        feature, status: done, priority: medium, super-phase: null, created:
        2026-06-24T16:21:04.782Z, completed: 2026-06-24T17:15:42.772Z, notes: ['currently
            we can add notes, but often times those mark sub-tasks. when the agent
            runs, they often break the task out into multiple tasks, which pollutes
            the todo list. we would like to manage chile task items with independent
            statuses so that we can easily see the fine-status of the todo item as
            the agent runs', I wonder if task status should be dynamically
            calculated from its child tasks?, we'll need to update the CLI
            interface and the skill instructions, I wonder if we want both notes
            and tasks or just replace notes with tasks...?, design approved →
            docs/plans/2026-06-24-sub-tasks-design.md (child tasks + derived
            calc-status; notes kept; dots are UI scope), impl plan →
            docs/plans/2026-06-24-sub-tasks-implementation.md (9 TDD tasks +
            pytest), 'shipped child tasks + auto-maintained calc-status; CLI: tasks
            / task add|status|rm + lifecycle shortcuts; render in get/list; pytest
            suite (28 tests). Web TODO drawer (claude-tmux-manager todos.ts) render
            is a tracked follow-up.']}, {id: phased-tasks, title: PHASED-TASKS,
        type: feature, status: done, priority: medium, super-phase: null,
        created: 2026-06-24T17:17:41.583Z, completed: 2026-06-24T17:29:26.890Z,
        notes: ['I would like the ability to assign a phase # to tasks, and auto-soft
            by phase#', "Design (user-approved): phase lives on CHILD TASKS, not across
            items. Rename item-level 'phase' field → 'super-phase' (passive/displayed,
            frees the name). Add task phase via 'task add --phase N' + 'task phase
            <item> <i> <N|none>'; child tasks stored auto-sorted by phase (unphased
            last, stable within phase) so [i] indices match the sorted display. calc-status
            unaffected.", "Shipped in 6c9e0ca: task-level 'phase' (task add --phase
            / task phase <i> <N|none>), auto-sort by phase (unphased last, stable),
            render phase column; item 'phase'→'super-phase' (this file migrated).
            40 tests green; SKILL.md re-installed."]}]
